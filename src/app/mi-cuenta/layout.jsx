export const metadata = {
  title: 'Mi Cuenta',
  description: 'Tu panel personal: estado de tus productos y accesos directos.',
  robots: { index: false, follow: true },
};

export default function MiCuentaLayout({ children }) {
  return children;
}
